#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
export DOCX_COMMENTS_PROG="${0##*/}"
export PYTHONDONTWRITEBYTECODE=1
exec "$SCRIPT_DIR/docx-comments" --mode docx2md "$@"
